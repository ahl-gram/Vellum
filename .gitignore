# No trailing slash, deliberately. `node_modules/` matches only a DIRECTORY, and the
# throwaway-worktree recipe in .claude/agents/vellum-guard-prover.md creates node_modules
# as a SYMLINK to the parent checkout's. Git sees a symlink as a file, so the slashed form
# let it straight through, and a `git add -A` from such a worktree committed a link whose
# contents are an absolute path on one machine. Caught on PR #369, 2026-08-12.
node_modules
out/
*.log
.DS_Store

# Built deploy artifact (assembled by `npm run build`, published by deploy.yml).
dist/

# Astro's generated types/cache dir (#203).
.astro/

# Generated runtime trees under public/ (#204, #205): the tsc engine emit, the
# Vite bundle twins (#163, #208), and the atlas/gallery showcases, regenerated by
# `npm run astro:generate` before every astro dev/build. Committed app-surface
# SOURCE in public/ is tracked (public/ is its single home since Sub 5, #206).
public/explorer/engine/
public/explorer/app.bundle.js
public/explorer/worker.bundle.js
public/explorer/chunks/
public/print-room/app.bundle.js
public/seed-of-the-day/app.bundle.js
public/reading-room/app.bundle.js
public/prospect/app.bundle.js
public/ribbon/app.bundle.js
public/specimen/app.bundle.js
public/app.bundle.js
public/atlas/
public/gallery/

# The discovery files (#286), generated from NAV_ITEMS by the same step so they
# cannot drift from the nav.
public/sitemap.xml
public/robots.txt
public/llms.txt

.claude/settings.local.json
# Transient review worktrees spawned by Claude Code workflows.
.claude/worktrees/
SESSION-NOTES.md
SESSION-NOTES-ARCHIVE-*.md
RESUME-HERE.md
# CLAUDE.md was local-only until 2026-08-08 and is now TRACKED (it is project
# instructions, not personal notes). RESUME-HERE and the session notes stay
# ignored: they are per-session state, not instructions.

# Local planning docs, no longer tracked. Backlog lives in GitHub issues.
roadmap/
