node_modules/
out/
*.log
.DS_Store

# Built deploy artifact (assembled by `npm run build`, published by deploy.yml).
dist/

# Astro's generated types/cache dir (#203).
.astro/

# Generated runtime trees under public/ (#204, #205): the tsc engine emit, the
# Vite bundle twins (#163, #208), and the atlas/gallery showcases, regenerated by
# `npm run astro:generate` before every astro dev/build. Committed app-surface
# SOURCE in public/ is tracked (public/ is its single home since Sub 5, #206).
public/explorer/engine/
public/explorer/app.bundle.js
public/explorer/worker.bundle.js
public/explorer/chunks/
public/print-room/app.bundle.js
public/seed-of-the-day/app.bundle.js
public/atlas/
public/gallery/

.claude/settings.local.json
# Transient review worktrees spawned by Claude Code workflows.
.claude/worktrees/
SESSION-NOTES.md
SESSION-NOTES-ARCHIVE-*.md
RESUME-HERE.md
# Personal working-style instructions for Claude Code (local only, not published).
CLAUDE.md

# Local planning docs, no longer tracked. Backlog lives in GitHub issues.
roadmap/
